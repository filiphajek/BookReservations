<?xml version="1.0" encoding="utf-8" ?>
<vw:BaseContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:vw="clr-namespace:BookReservations.App.Views"
                    xmlns:vm="clr-namespace:BookReservations.App.ViewModels"
                    x:DataType="vm:SettingsViewModel"
                    x:Class="BookReservations.App.Views.SettingsPage"
                    xmlns:t="clr-namespace:BookReservations.App.Resources.Texts"
                    BackgroundColor="{StaticResource PrimaryColor}"
                    Title="{x:Static t:ShellTexts.Settings_Title}">
    <VerticalStackLayout>
        <Picker x:Name="picker" Title="Language" Margin="5" SelectedItem="{Binding SelectedLanguage}">
            <Picker.Items>
                <x:String>CS</x:String>
                <x:String>EN</x:String>
            </Picker.Items>
        </Picker>
    </VerticalStackLayout>
</vw:BaseContentPage>